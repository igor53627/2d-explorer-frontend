<style>
.bridge-intent-card .detail-row {
    display: flex;
    flex-wrap: wrap;
    gap: 8px 16px;
    padding: 10px 0;
    border-bottom: 1px solid var(--border-color, #e9ecef);
}
.bridge-intent-card .detail-row:last-child {
    border-bottom: none;
}
.bridge-intent-card .detail-label {
    flex: 0 0 160px;
    color: var(--text-muted);
    font-size: 13px;
}
.bridge-intent-card .detail-value {
    flex: 1 1 240px;
    min-width: 0;
}
</style>